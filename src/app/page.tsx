import Image from "next/image";

export default function Home() {
  return (
    <div>
      <header className="flex flex-row justify-between mx-auto px-4 py-3 sm:px-6 bg-[var(--secondary)] w-full">
        <a href="/">Home</a>
      </header>
    </div>
  );
}
